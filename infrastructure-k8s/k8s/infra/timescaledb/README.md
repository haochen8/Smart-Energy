# TimescaleDB (Dev / Student Setup)

Minimal TimescaleDB (PostgreSQL with time-series extensions) as a StatefulSet with a ClusterIP Service. Intended to store raw meter readings and algorithm outputs. For production, prefer an operator or managed PostgreSQL/Timescale service.
